version: "3.3"
services:
    core:
        build: './core'
        container_name: 'core'
        restart: 'always'
        expose:
            - 8000
    nginx:
        build: './nginx'
        container_name: 'nginx'
        restart: 'always'
        ports:
            - 443:443